# TCPsocketProgramming
This project demonstrates basic TCP socket programming where a server waits for a client connection, receives a message, and sends a reply. It uses socket(), bind(), listen(), accept(), send(), and read() functions.
